developer=https://nextcloud.com/
url=${developer}
maintainer=http://indiecomputing.com/
pkgname=$(basename $(pwd))
pkgver=2021.12.20
# NOTE: must update integer in URL below
pkgrel=3
pkgdesc="The Nextcloud password app"
_app=nextcloud
_acc=passwords
arch=('any')
license=("AGPL3")
source=("${_acc}-${pkgver}.tar.gz::https://git.mdns.eu/nextcloud/${_acc}/-/jobs/13439/artifacts/raw/${_acc}.tar.gz")
releasepage=("https://apps.nextcloud.com/apps/${_acc}")
depends=(
    'nextcloud')
options=('!strip')
sha512sums=('125cb536391dafcb825d0918508fc59846c70a73e053b8a1987f3af7a28f98fc1ffcabd90bb2bbf4cb14faa89dfccdffefd8a94a9b664dfae4c7abc8ea1ea362')

package() {
# Manifest
    mkdir -p -m 0755 ${pkgdir}/ubos/lib/ubos/manifests
    sed -e "s!@ACCESSORYID@!$_acc!g" ${startdir}/../shared/ubos-manifest-accessory.tmpl.json > ${pkgdir}/ubos/lib/ubos/manifests/${pkgname}.json
    chmod 0644 ${pkgdir}/ubos/lib/ubos/manifests/${pkgname}.json

# Icons
#    install -D -m644 ${startdir}/appicons/{72x72,144x144}.png ${pkgdir}/ubos/http/_appicons/${pkgname}/

# Code
    mkdir -p ${pkgdir}/ubos/share/${_app}/apps
    cp -dr --no-preserve=ownership ${srcdir}/${_acc} ${pkgdir}/ubos/share/${_app}/apps/
}
